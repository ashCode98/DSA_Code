#include<iostream>
#include<unordered_set>
using namespace std;

int main(){
    unordered_set<int> s;
    s.insert(6);
    s.insert(77);
    s.erase(6);
    /*
    if(s.find(8)==s.end()){
       cout << "Not found the number";
    }
    else{
       cout << "Hurry, got it";
    } 
    */

    for(auto it = s.begin(); it != s.end(); it++){
       cout << *it << " "; 
    }
   
    return 0;
}
